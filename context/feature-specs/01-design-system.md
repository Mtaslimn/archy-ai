Read `AGENTS.md` before starting.

we're adding the design system and UI primitive components.

Install and configure `shadcn/ui`.

add these shadcn components:
-Button
-Card
-Dialog
-Input
-Tabs
-Textarea
-ScrollArea

Do not modify the generated `components/ui/*` files after installation. 

Also Install `lucid-react`. 

create `lib/utils.ts` with a resuable `cn()` helper for merging Tailwind classes.

Ensure all components match the existing dark theme in `globals.css`.

### Check when done

- All componets import without errors 
- `cn()` works properly
- No default light sytling appears

